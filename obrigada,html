<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Obrigado!</title>
    <style>
        body {
            font-family: 'Segoe UI', Arial, sans-serif;
            background: linear-gradient(135deg, #f8f9fc, #eef1f7);
            margin: 0;
            color: #333;
        }

        /* HEADER MODERNO */
        header {
            background: linear-gradient(135deg, #2F55A4, #759fd6);
            color: white;
            padding: 45px 20px;
            text-align: center;
            box-shadow: 0 8px 20px rgba(0,0,0,0.1);
        }

        header h1 {
            margin: 0;
            font-size: 32px;
            font-weight: 600;
        }

        header p {
            margin-top: 8px;
            font-size: 16px;
            opacity: 0.95;
        }

        /* CARD CENTRAL */
        main {
            max-width: 650px;
            margin: 80px auto;
            background: white;
            padding: 50px 40px;
            border-radius: 15px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.1);
            text-align: center;
        }

        h2 {
            color: #f79d3c;
            margin-bottom: 20px;
            font-size: 28px;
        }

        p {
            font-size: 18px;
            color: #555;
            line-height: 1.6;
        }

        /* BOTÃO MODERNO */
        a {
            display: inline-block;
            margin-top: 30px;
            padding: 14px 28px;
            background: linear-gradient(135deg, #f7b267, #f79d3c);
            color: white;
            text-decoration: none;
            border-radius: 30px;
            font-weight: 600;
            font-size: 16px;
            transition: 0.3s;
        }

        a:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(0,0,0,0.15);
        }

        /* FOOTER */
        footer {
            text-align: center;
            color: #555;
            margin-top: 50px;
            font-size: 14px;
        }
    </style>
</head>
<body>

<header>
    <h1>Plataforma de Doação & Voluntariado ❤️</h1>
    <p>Conectando pessoas, recursos e solidariedade.</p>
</header>

<main>
    <h2 id="mensagemPrincipal">Doação Recebida</h2>
    <p>Sua doação foi registrada com sucesso! Você está ajudando a transformar vidas. 🙌</p>
    <a href="doar.html">Voltar ao Início</a>
</main>

<script>
    // Pegar o item doado da URL
    const params = new URLSearchParams(window.location.search);
    const item = params.get('item') || "Item";

    // Alterar o título principal para mostrar o item doado
    const h1 = document.getElementById('mensagemPrincipal');
    h1.innerText = `Obrigado por doar: ${item}! ❤️`;
</script>

<footer>
    &copy; 2026 Plataforma de Doação e Voluntariado – Projeto Integrador
</footer>


</body>
</html>
